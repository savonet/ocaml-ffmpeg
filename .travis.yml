language: c
sudo: false
addons:
  apt:
    packages:
    - ocaml-nox
    - ocaml-findlib
    - libavutil-dev
    - libavformat-dev
    - libavcodec-dev
    - libswresample-dev
    - libswscale-dev

script: ./bootstrap && ./configure && make
